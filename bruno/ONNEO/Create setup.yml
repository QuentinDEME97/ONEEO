info:
  name: Create setup
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:3000/api/setup
  body:
    type: json
    data: |-
      {
        spaceName: "Oneeo"
        firstName: "John"
        lastName: "Doe"
        email: "john.doe@example.com",
        password: "Test1234",
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
