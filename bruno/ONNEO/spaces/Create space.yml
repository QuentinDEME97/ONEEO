info:
  name: Create space
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:3000/api/spaces
  body:
    type: json
    data: |-
      {
        "name": "Test"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
